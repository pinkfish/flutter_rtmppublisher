name: rtmppublisher_example
description: Demonstrates how to use the rtmppublisher plugin.

publish_to: 'none'

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  rtmppublisher:
    path: ../

  path_provider: ^1.6.7
  cupertino_icons: ^0.1.3
  video_player: ^0.10.9+1
  e2e: ^0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0
  rtmppublisher:
    path: ../

flutter:
  uses-material-design: true